apiVersion: 1

providers:
  - name: GAIA Sensors
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    ports:
      - "3001:3001"
    options:
      path: /etc/grafana/provisioning/dashboards
